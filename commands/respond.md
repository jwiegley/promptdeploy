---
description: Draft a Markdown report answering every open reviewer comment on a PR, instead of replying on GitHub
---

In PR $ARGUMENTS there are several open comments from my colleagues. I want you to address all of them, but instead of commenting in response, I want you to create a Markdown report containing the answer you would give to each comment’s author for every one of these comments to explain your fix or your clarification in response.

See also -- comment-triage chain: `assess` analyzes reviewer comments; `respond` (this one) drafts written replies to each; `markdown` formats those replies as GitHub suggestion blocks; `bugbot` fixes and resolves automated bot comments. Run them in that order.
